# {问题编号}: 解决方案

## 修复策略

{方案核心思路，一段话说明}

## 前置确认

- [ ] 已确认分支策略（默认单审计分支串行集成）
- [ ] 若启用审计分支，已创建 `audit/YYYY-MM-DD`
- [ ] 确认相关模块已导入（如 QTimer、threading、logging 等）
- [ ] 确认修改不影响其他调用者
- [ ] 确认无循环依赖风险
- [ ] 确认修复模式与项目现有代码风格一致

## 修改计划

| 文件 | 行号 | 修改内容 | 风险等级 |
|------|------|---------|---------|
| {file1} | L{N} | {变更描述} | 低/中/高 |

## 代码变更

### 修改1：{描述}

```python
# 修改前
{old_code}

# 修改后
{new_code}
```

### 修改2：{描述}

（同上格式）

## 验证计划

| 步骤 | 方法 | 预期结果 |
|------|------|---------|
| 1. 编译验证 | `python -m py_compile {file}` | 退出码 0 |
| 2. 残留检查 | `grep_search` 搜索原问题模式 | 当前文件无匹配 |
| 3. 导入检查 | 确认新增依赖已导入 | 无缺失 |
| 4. 功能测试 | {具体测试方法} | {预期结果} |

## 回退方案

```bash
git revert {commit_hash}
```

## 分支与提交流程

- 默认：在同一审计分支内串行修复
- 提交粒度：`1问题 = 1提交`
- 合并策略：完成验证后一次性合并回主分支
- 约束：禁止 `git add .` / `git add -A`，必须逐文件添加

## 提交信息模板（建议）

```text
fix(P{优先级}-{问题编号}): {一句话修复摘要}

- root-cause: {根因}
- change: {关键修改点}
- verify: {验证结果}
```

## 注意事项

- {修复过程中需要特别注意的点}
